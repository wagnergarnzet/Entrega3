name: Deploy APIs to Azure

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        app-name: [consumer, consumerdelete, consumerupdate, producer]
        app-path: [Fiap2025.Entrega3.Consumer, Fiap2025.Entrega3.ConsumerDelete, Fiap2025.Entrega3.ConsumerUpdate, Fiap2025.Entrega3.Producer]
        azure-webapp-name: "${{ secrets.AZURE_WEBAPP_NAME_CONSUMER }}, ${{ secrets.AZURE_WEBAPP_NAME_CONSUMERDELETE }}, ${{ secrets.AZURE_WEBAPP_NAME_CONSUMERUPDATE }}, ${{ secrets.AZURE_WEBAPP_NAME_PRODUCER }}"
        azure-publish-profile: "${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_CONSUMER }}, ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_CONSUMERDELETE }}, ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_CONSUMERUPDATE }}, ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_PRODUCER }}"

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore ${{ matrix.app-path }}

    - name: Build
      run: dotnet build ${{ matrix.app-path }} --configuration Release --no-restore

    - name: Publish
      run: dotnet publish ${{ matrix.app-path }} --configuration Release --output ./publish --no-build

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ matrix.azure-webapp-name }}
        publish-profile: ${{ matrix.azure-publish-profile }}
        package: ./publish
